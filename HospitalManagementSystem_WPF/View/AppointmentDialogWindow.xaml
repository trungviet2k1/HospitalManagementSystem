<Window x:Class="HospitalManagementSystem_WPF.View.AppointmentDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HospitalManagementSystem_WPF"
        Title="Schedule An Appointment" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#f5f5f5">

    <Window.Resources>
        <!-- Converters -->
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>

        <!-- Styles -->
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderBrush" Value="#ccc"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="10" Background="White" Padding="20" Margin="10" BorderBrush="#ddd" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Form Grid -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Patient Selection -->
                <TextBlock Text="Patient:" Grid.Row="0" Grid.Column="0" FontWeight="Bold"/>
                <ComboBox Grid.Row="0" Grid.Column="1"
                          ItemsSource="{Binding AvailablePatients}"
                          DisplayMemberPath="FullName"
                          SelectedValuePath="PatientId"
                          SelectedValue="{Binding Appointment.PatientId}"
                          IsEnabled="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <!-- Doctor Selection -->
                <TextBlock Text="Doctor:" Grid.Row="1" Grid.Column="0" FontWeight="Bold"/>
                <ComboBox Grid.Row="1" Grid.Column="1"
                          ItemsSource="{Binding AvailableDoctors}"
                          DisplayMemberPath="FullName"
                          SelectedValuePath="UserId"
                          SelectedValue="{Binding Appointment.DoctorId}"/>

                <!-- Appointment Date -->
                <TextBlock Text="Appointment Date:" Grid.Row="2" Grid.Column="0" FontWeight="Bold"/>
                <DatePicker Grid.Row="2" Grid.Column="1"
                            SelectedDate="{Binding Appointment.AppointmentDate, Mode=TwoWay}"/>

                <!-- Appointment Time -->
                <TextBlock Text="Appointment Time:" Grid.Row="3" Grid.Column="0" FontWeight="Bold"/>
                <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                    <ComboBox Width="80" Margin="0,0,10,0"
                              ItemsSource="{Binding AvailableHours}"
                              SelectedItem="{Binding SelectedHour}"/>
                    <TextBlock Text=":" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <ComboBox Width="80"
                              ItemsSource="{Binding AvailableMinutes}"
                              SelectedItem="{Binding SelectedMinute}"/>
                </StackPanel>

                <!-- Current DateTime Display -->
                <TextBlock Text="Scheduled for:" Grid.Row="4" Grid.Column="0" FontWeight="Bold"/>
                <TextBlock Grid.Row="4" Grid.Column="1"
                           Text="{Binding DisplayDateTime}"
                           FontStyle="Italic"
                           Foreground="Blue"/>

                <!-- Reason -->
                <TextBlock Text="Reason:" Grid.Row="5" Grid.Column="0" FontWeight="Bold"/>
                <TextBox Grid.Row="5" Grid.Column="1"
                         Text="{Binding Appointment.Reason, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         Height="60"
                         VerticalScrollBarVisibility="Auto"/>

                <!-- Status Message -->
                <TextBlock Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2"
                           Text="{Binding StatusMessage}"
                           Foreground="Red"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap"
                           Visibility="{Binding HasStatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>

            <!-- Buttons -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK" 
                        Background="#4CAF50" 
                        Foreground="White" 
                        IsDefault="True" 
                        Command="{Binding OkCommand}"/>
                <Button Content="Cancel" 
                        Background="#F44336" 
                        Foreground="White" 
                        IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>